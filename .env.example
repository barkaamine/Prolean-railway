# PostgreSQL Database Configuration (Railway)
# Get these values from your Railway PostgreSQL service dashboard

# Option 1: Use DATABASE_URL (Recommended - Railway provides this automatically)
DATABASE_URL=postgresql://user:password@host:port/database

# Option 2: Use individual credentials (if you prefer)
# PGHOST=your-railway-host.railway.app
# PGPORT=5432
# PGDATABASE=railway
# PGUSER=postgres
# PGPASSWORD=your-password

# Django Settings
SECRET_KEY=django-insecure-j8^d-$zd((x5o&qi&j=8k9-9)p5#xyb=h5ty0yl^a0ma&gvhy5
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# For Production (set these in Railway)
# DEBUG=False
# ALLOWED_HOSTS=your-domain.com,*.railway.app
